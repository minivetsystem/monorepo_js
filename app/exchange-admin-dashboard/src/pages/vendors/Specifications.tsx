import React, { FC, ReactElement } from 'react';
import { Box, Grid } from '@mui/material';

const Specifications: FC = (): ReactElement => {
  return (
    <Box>
      <Grid container>
        <Grid item xs={12}></Grid>
      </Grid>
    </Box>
  );
};

export default Specifications;
